# BẢNG AUDIT - 06/08/2026

> Anh Luân 06/08: *"1- bỏ v6, ko được làm ảnh hưởng v5. 2- okey em, tồn thì làm đi."*

Chạy trọn `GIAO_THUC_AUDIT.md` - 9 mảng, sau khi gỡ hẳn bản v6 khỏi nguồn.
`./verify.sh` **32 bộ kiểm, 23 phút 51, XANH HẾT.**

Cách đọc: mảng nào **XANH** là máy đo được và đo sạch. Mục 9 khai thẳng phần **chưa đo được** -
và trong đợt này mục 9 có thêm ba quan sát mới, chưa sửa, để anh quyết.

---

## Bảng tổng kết

| # | Mảng | Kết quả | Đo bằng |
|---|---|---|---|
| 1 | Giao diện | **XANH** | `_checkui` 1048 lượt mở thật · `_checkmat` 1066 chuỗi chữ · `_checkux` 211 · `_check18` 179 (90 trang/tab) · `_tall` 225 icon · **`_checkdrawer` 25 ngăn kéo** · **`_checkcrumb` 3 khổ màn** |
| 2 | Nghiệp vụ (phủ SOP) | **XANH** | `check_sop.py` ĐẠT - 357 cột · 93 trigger HD3 · 51 chỉ số BC2 · 31 hành động CH3 · 12 màn VH + 9 bảng BC |
| 3 | Chức năng | **XANH** | `_checknv` · `_checkbam` · `_checknguoi` · `_check12` 37 · `_check17` 413 · `_checkneo` 87 bước · `_checktour` · `_checkqa` 171 |
| 4 | Dữ liệu demo + nút Reset | **XANH** | `check_logic.py` ĐẠT · `check_data.py` ĐẠT · `_checkdata` 27 luật / 6376 lượt · `_checkdemo` 7×9 · `_checkreset` 15 |
| 5 | Ngữ cảnh (câu chữ, drawer, nhật ký) | **XANH** (sau khi vá 1 chỗ) | `_checkaudit` 53 · `_checkux` 87 form · `_check13` 174 · `_check15` 39 cửa ghi · **đọc tay 60 câu giải thích** |
| 6 | Phạm vi dữ liệu & rò rỉ | **XANH** (sau khi sửa 1 cái thước) | `_checkmien` 0/0 · `_checknguoi` · `_check12` |
| 7 | Nhất quán ba cổng | **XANH** | `_check14` 215 · `_check16` 709 |
| 8 | Tài liệu theo kịp | **XANH** | `_checkaudit` canh README_SRC nhắc đủ 32 bộ kiểm |
| 9 | Chưa đo được | **KHAI THẲNG** | xem mục 9 |
| + | Chuỗi phối hợp nhiều người | **XANH** | `_checkchuoi` 31 mắt xích / 6 chuỗi |
| + | Không mời rồi đuổi | **XANH** | `_checkmoi` 1019 tiêu chí / 32 người |
| + | Không nói hai lần | **XANH** | `_checklap` 856 tiêu chí / 16 chức danh |

---

## Việc 1 - GỠ HẲN BẢN V6

Đã gỡ khỏi nguồn: cờ `ITTS_V6` · hàm `V6()` · bảng `NAVTREE6` · `v6Dap` · toàn bộ máy đổi bản
của nút Đổi cổng (`congLaV6`/`congURLBan`/`congDiBan`/`congBanNho`/`congBanGhi`) · 25 nhánh rẽ ·
11 nhánh trong 7 bộ kiểm · hai dòng build ở `gen_v5.py` và `extract_js.py`.

`_check11` nay có **8 tiêu chí chặn v6 quay lại** - cùng kiểu `check_gs.py` canh lớp Google
Sheets đã nghỉ hưu.

### Ba chỗ em quyết lệch khỏi "xoá sạch" - và lý do

**1. GIỮ trang "Bàn làm việc".** Em định xoá vì nó là màn riêng của v6: không có trong cây menu
V5, không nằm trong phạm vi chức danh nào. Đo lại thì không phải vậy - trang **Sổ người đồng
hành** của V5 bấm vào một dòng là gọi `phMo()` mở đúng trang đó với thực thể đặt sẵn là phụ
huynh. Nó đang làm **trang hồ sơ phụ huynh của V5**. Xoá là xoá một tính năng V5 đang chạy.
Nay nó xếp cùng nhóm `hoso`/`hosogv`/`hosonv`: trang chi tiết mở ra từ một danh sách.

> **Và nó đang hỏng sẵn:** vì không ở trong phạm vi ai cả, bấm một phụ huynh là ăn ngay dòng
> "Trang ngoài phạm vi chức danh của bạn" - **cùng họ với 67 chỗ "mời rồi đuổi" đã vá 05/08**,
> chỉ khác là lối đi vòng qua một hàm nên bộ kiểm dò theo `go('...')` không thấy. Đã vá.

**2. KHÔNG xoá hai phép đo từng đo cây menu v6** - chuyển chúng sang đo cây V5:
- "menu không làm mất đường tới trang nào"
- "mọi sổ Tra cứu thuộc về một thực thể"

Chính việc chuyển đó lộ ra **hai sổ chưa ai hỏi tới** (`khoahoc`, `nhanvien`) - cây v6 có 15 sổ,
cây V5 có 18. Đã khai lý do ở nguồn.

**3. SỬA THƯỚC, KHÔNG SỬA CÂU CHỮ APP.** Xem mảng 6.

---

## Những chỗ hỏng bắt được trong đợt này

### Do chính việc gỡ v6 gây ra (bắt được và vá trong cùng ngày)
- **Chốt chặn v6 bắt được chính em**: nguồn vẫn còn `cong-nhan-vien-v6` trong hai biểu thức tính
  đường dẫn cổng. Cái chốt cắm để canh ba tháng nữa thì nó bắt sau ba tiếng.
- **`_checkmien` báo 5 người rò dữ liệu miền tiền** - xem mảng 6.

### Mảng 5 - đọc tay 60 câu giải thích trong form, bắt được một chỗ
> Màn **Cài đặt → Dữ liệu demo** hiện câu *"Bộ dữ liệu này chưa ghi ngày sinh - không tự dịch
> được. **Chạy lại pipeline** để có mốc."*
>
> "Chạy lại pipeline" là việc của người làm app. Người mở màn Cài đặt là quản trị viên của trung
> tâm - họ không có pipeline nào để chạy, đọc xong không biết làm gì tiếp. Nay câu nói rõ **hậu
> quả** (lịch tuần và các hạn hiện theo ngày gốc) và **lối ra có thật** (bấm "Dựng lại demo" ở
> khối ngay trên).

Còn lại 59 câu đọc thuận: nói hậu quả nếu bỏ trống, nói hạn lấy từ tham số nào, nói số điện thoại
này cũng là số đăng nhập cổng phụ huynh.

### Mảng 6 - THƯỚC SAI, KHÔNG PHẢI APP RÒ
`_checkmien` báo **5 người** (4 giáo viên + Trưởng phòng ACA) nhìn thấy dữ liệu miền tiền - đúng
chỗ đau nhất, và xuất hiện đúng lúc em vừa cho trang Bàn làm việc vào diện ai cũng xem được.

Luật số 1 của giao thức là **nghi cái thước trước**. Đọc câu bị bắt:

> "Buổi **còn nợ** nhận xét"

Đó là buổi giảng viên chưa viết nhận xét - không liên quan gì tới tiền. Bộ kiểm có chữ `còn nợ`
trong danh sách từ khoá miền tiền nên chấm oan. Trước hôm nay không lộ vì trang chứa câu đó nằm
ngoài phạm vi mọi chức danh.

**Đã sửa thước, không sửa câu chữ của app.** Đổi câu cho vừa lòng một biểu thức là làm hỏng tiếng
Việt của sản phẩm để cái máy đỡ kêu.

---

## Ba việc TAY mà giao thức bắt phải làm

### (1) Đóng vai từng chức danh, mở app, đi một việc

Sáu người, không ai gặp màn trắng, **không lỗi JS nào** trong cả lượt:

| Chức danh | Trang đáp | Chuông | Nút làm được ngay |
|---|---|---|---|
| NV Tư vấn | Trang bắt đầu | 11 việc | **1** |
| NV Học vụ | Xếp lớp & Onboarding | 66 việc | 2 |
| TP Kế toán | Tuyển sinh | 27 việc | 48 |
| Giáo viên | Học tập | 4 việc | 11 |
| TP ACA | Học tập | 50 việc | **1** |
| CEO | Chỉ số & KPI | 220 việc | 1 |

Chuông đếm **đúng** cho từng người - không ai bị bỏ quên. Xem mục 9 về cột cuối.

### (2) Bấm Reset demo rồi rà lại
`_checkreset` 15 tiêu chí ĐẠT: dữ liệu về đúng trạng thái chuẩn, **cấu hình giữ nguyên**.
`_checkdemo` sau reset: 7 thứ trong tuần × 9 chức danh, không thứ nào mở app thấy bảng trống.

### (3) Đọc câu chữ ngăn kéo và form như người dùng lần đầu
Xem mảng 5 ở trên - 60 câu, bắt được 1 chỗ.

---

## 9. NHỮNG THỨ MÁY KHÔNG ĐO ĐƯỢC - khai thẳng

### 9a. Ba quan sát MỚI trong đợt này - chưa sửa, chờ anh quyết

**1. Hai chức danh phải đi vòng hơn hẳn để chạm được việc của mình.**
NV Tư vấn có 11 việc đang chờ mà trang đáp chỉ có **một** nút bấm làm được; TP ACA có 50 việc,
cũng một nút. Toàn bộ màn còn lại là chip lọc và ô số - họ phải bấm chip → ra danh sách → bấm
một dòng → sang trang → mới làm được. So với TP Kế toán mở ra thấy 48 nút làm được ngay.
*Chưa gọi là lỗi:* `_checknv` đo được app vẫn ghi thật qua đúng đường đó. Nhưng đây là loại thứ
máy đo được "có việc" mà không đo được "một ngày có trôi chảy không".

**2. `_checkngay` ghi chú:** NV Marketing mở hồ sơ ra thấy **86 việc của người khác** so với 26
việc của chính họ (gấp 3,3 lần). Con số này nằm trên Bàn làm việc - màn nay là trang hồ sơ phụ
huynh của V5. Việc của bộ phận khác **có ghi rõ đang chờ ai** (`_checkaudit` canh), nên không
phải rò rỉ; nhưng tỷ lệ 3,3 lần là một màn hình phần lớn không phải của người đang xem.

**3. `_checknguoi` ghi chú:** Trịnh Quốc Bảo [giáo viên · Cơ sở 3] có **1 mục menu mở ra TRỐNG**
- trang Giao việc. Một mục menu mở ra trống là một lời mời hụt.

### 9b. Những giới hạn cũ, vẫn đúng
- **Câu chữ có tự nhiên không.** Máy đếm được "có câu giải thích", không đọc được "câu này có dễ
  hiểu với người lần đầu không". Đợt này em đọc tay 60 câu - vẫn là mắt em, không phải mắt người
  dùng thật.
- **Đẹp.** `_checkui`/`_checkmat` đo "không vỡ, không cắt chữ, không che nhau" - không đo được
  nhịp đọc, không đo được "nhìn có chuyên nghiệp không".
- **Nhịp làm việc thật.** Xem 9a.1.
- **Giới hạn của bản demo:** không backend · dữ liệu nằm trong localStorage từng máy · phân quyền
  thi hành ở trình duyệt · dữ liệu là dữ liệu mẫu.

### 9c. Bài học về CHÍNH BỘ KIỂM - ba lần trong hai ngày

Cả ba lần một bộ kiểm **báo xanh oan**, và cả ba đều suýt làm em giao hàng sai:

| Lần | Bộ kiểm | Sai ở đâu |
|---|---|---|
| 05/08 | `_checkmoi` | Đo trên chuỗi **thô** của `RENDER[k]()`, mà bộ lọc nằm ở `go()` |
| 05/08 | `_checklap` (bản node) | Trong node `banAiHTML()` trả rỗng - đếm 0 thanh ở **cả hai** bản |
| 06/08 | `_checkmien` | Chữ "còn nợ" bắt cả "còn nợ **nhận xét**" - chấm oan 5 người |

**Luật đã ghi vào `README_SRC.md`:** *bộ kiểm mới phải được thử ngược trên bản CŨ và phải ĐỎ ở
đó, trước khi được tin.* Một cái thước chỉ báo xanh mà chưa từng báo đỏ là một cái thước chưa ai
biết nó đo được gì.
