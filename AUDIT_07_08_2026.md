# BẢNG AUDIT - 07/08/2026

> Chạy trọn `GIAO_THUC_AUDIT.md` sau cụm **AC1-AC6** (sáu việc Trưởng phòng ACA đặt qua điện
> thoại 06/08) và các sửa anh Luân bắt trong ngày.
> Bản dựng: **`db27e2`** - đang chạy trên demo online.

Cách đọc: **XANH** là máy đo được và đo sạch. Mục 9 khai thẳng phần **chưa đo được** - và mục
này là phần đáng đọc nhất, vì nó nói cái mà tám mục kia không với tới.

---

## Bảng tổng kết

| # | Mảng | Kết quả | Đo bằng |
|---|---|---|---|
| 1 | Giao diện | **XANH** | `_checkui` · `_checkmat` 1214 chuỗi · `_checkux` 211 · `_checkdrawer` 27 ngăn kéo · `_checkcrumb` · `_tall` icon |
| 2 | Nghiệp vụ (phủ SOP) | **XANH** | `check_sop.py` ĐẠT - 357 cột · 93 trigger HD3 · 51 chỉ số BC2 · 31 hành động CH3 · 12 màn VH + 9 bảng BC |
| 3 | Chức năng | **XANH** | `_checknv` · `_checkbam` 187 lượt bấm thật · `_checknguoi` · `_check12` · `_check17` 413 · `_checkneo` · `_checktour` · `_checkqa` |
| 4 | Dữ liệu demo + nút Reset | **XANH** | `check_logic.py` ĐẠT · `check_data.py` ĐẠT · `_checkdata` 27 luật / 6977 lượt · `_checkdemo` 7×9 · `_checkreset` 15 |
| 5 | Ngữ cảnh (câu chữ, chú thích, drawer) | **XANH** | `_checkux` 90 form ghi đều có lời giải thích · `_checkaudit` 53 · `_checkmien` 0/0 |
| 6 | Phạm vi dữ liệu theo chức danh | **XANH** | `_checkmien` 0/0 · `_checknguoi` · `_checkmoi` (không mời rồi đuổi) |
| 7 | Nhất quán ba cổng | **XANH** | `_check14` cổng học viên · `_checklap` 884 tiêu chí / 16 chức danh |
| 8 | Tài liệu | **XANH** | `_checkaudit` canh `README_SRC` nhắc đủ **33 bộ kiểm** |
| 9 | **Khai thẳng phần chưa đo được** | xem dưới | - |

**Bộ kiểm mới sinh ra trong đợt này:** `_checkdem.js` - *số trên thẻ phải tìm được ở danh sách*
(105 tiêu chí / 26 lớp). Đã thử ngược trên bản cũ: **18/88 đỏ**.

---

## Sáu việc Trưởng phòng ACA đặt - làm gì, đo ra sao

| Việc | Đã làm | Số đo thật |
|---|---|---|
| **AC1** Trục lớp 1-1 vs nhóm | Thẻ 1-1/Nhóm trước tên lớp ở 5 chỗ, bộ lọc riêng, 4 lớp 1-1 trong demo | suy từ sĩ số tối đa = 1, một hàm `lopLa11()` dùng chung |
| **AC2** Hai tỷ lệ đạt AIM | Màn *Kết quả đầu ra & AIM*: hai tỷ lệ cạnh nhau, bóc theo GV / lớp / cơ sở / loại lớp; bảng 4 kỹ năng + Overall ở cả hai mốc; lịch sử thi trong ngăn kéo | DL18b (kỳ thi thật) tách bảng riêng vì học viên thi lại là chuyện thường |
| **AC3** Giờ dạy thực tế | Bảng công: hai ô đếm + cột *Chia 1-1 / nhóm*, lọc theo cơ sở | 30.1h = 5.8h (1-1, 19%) + 24.3h (nhóm, 81%) - cộng khớp tuyệt đối |
| **AC4** Buổi thi | Nhãn **MIDTERM** / **FINAL TEST**, cả viên buổi đổi viền và nền; nút *Đổi buổi thi* | 26 lớp đủ hai mốc; giáo viên `canAct=false` |
| **AC5** Lịch sử đổi lịch | Bảng lịch sử ngay dưới dải buổi + nút *Dời khóa học* có xem trước | dời buổi 3 lùi 5 ngày → **đúng 12 buổi** dời, lịch sử 1 → 2 dòng |
| **AC6** Rubric nhận xét | 5 tiêu chí chấm 1-5 + 5 ô tích, giữ nguyên ô văn xuôi | chấm 5-4-3 bỏ trống 2 → TB **4/5**, ô trống không tính vào mẫu số |

---

## Ba con bọ nặng lôi ra được trong đợt này

**1. Tab "Buổi học & điểm danh" chết câm.** Một biến khai sai phạm vi làm tab CHÍNH của trang
Vận hành lớp ném lỗi và không vẽ ra gì. Không bộ kiểm nào trước đó bấm vào đúng tab ấy, nên nó
hỏng mà không ai báo. `_checkdem` bắt được **ngay lượt chạy đầu tiên**.
→ *Một tab không có bộ kiểm nào bấm vào thì hỏng bao lâu cũng không ai biết.*

**2. Trang mới chưa hề có trên cây menu.** Màn *Kết quả đầu ra* vào được bằng link nhưng không
ai tìm thấy. Lượt verify trọn bộ bắt.

**3. Một việc có hai bản cài đặt.** Anh Luân bắt hai lần trong hai ngày: thẻ đếm N mà danh sách
dưới không có dấu nào. Gốc là **thẻ và bảng hỏi hai hàm khác nhau cho cùng một câu hỏi**. Đã
gom 8 chỗ về một hàm, và sinh `_checkdem` để canh.

---

## 9. KHAI THẲNG PHẦN CHƯA ĐO ĐƯỢC

### 9a. Ba cái bẫy đã cắn trong chính ngày hôm nay - đều là "đo một bản, sửa một bản khác"

| Bẫy | Hậu quả thật |
|---|---|
| `gen_v5.py` mặc định ghi `_src/`, `extract_js.py` mặc định đọc **gốc repo** | Em đọc nhầm kết quả của **bốn bộ kiểm**, tưởng đã vá xong trong khi bản đó chưa hề dựng lại. Đã cho hai mặc định khớp nhau. |
| Cùng bẫy ấy ở **chiều đẩy**: `update.sh` đọc gốc repo | Lần đẩy demo đầu tiên **hụt** - `update.sh` báo "không có thay đổi" và im lặng đúng nghĩa. Chốt cửa duy nhất là **đối chiếu mã bản dựng sau khi đẩy**. |
| Script sửa nhiều chỗ **dừng giữa chừng nhưng chỉ ghi file ở cuối** | Em báo "đã làm xong huy hiệu hai biểu tượng" - **sai**, không một sửa nào được lưu. Nay sửa từng bước, mỗi bước đếm lại ngay. |

**Chưa có máy canh ba cái bẫy này.** Chúng thuộc loại "quy trình làm việc", không phải "mã app".

### 9b. Một quyết định sai của em, đã tự sửa

Em từng **gieo một buổi nợ cả hai việc vào dữ liệu demo** để nhánh hai huy hiệu có chỗ hiện ra.
Sai: *"buổi đã dạy quá 24h mà không điểm danh"* là **lỗi dữ liệu thật**, ba bộ kiểm đều bắt
đúng. Gieo nó vào là **làm bẩn bộ demo để khoe một tính năng**, rồi phải đi khai miễn trừ ở từng
bộ kiểm - mà mỗi lần khai miễn trừ là một lần hạ thấp cái thước.
Chỗ đúng của ca đó là **trong bộ kiểm**: `_checkdem` D5 tự dựng ca trong bộ nhớ, đo xong trả lại
dữ liệu. Bộ demo sạch, tính năng vẫn có người canh.

### 9c. Hai lỗi cấu trúc anh Luân nêu, CHƯA sửa - để dành cho V2

1. **Cha - con đang ngược.** `Vận hành lớp` đứng trên menu, còn `Lớp học` - đáng ra là cha - lại
   bị ẩn. Nên vào thẳng màn vận hành rồi chọn lớp bằng ô xổ, thay vì *Lớp học → bấm một lớp →
   Vận hành lớp*. Cùng bệnh: `Hồ sơ giảng viên` chứa toàn bộ thông số một giảng viên nhưng chỉ
   mở được từ dòng trong danh sách.
2. **Một nghiệp vụ làm được ở nhiều nơi.** Anh Luân: *"cùng 1 nghiệp vụ mà ở bản hiện tại có thể
   làm được ở rất nhiều nơi, sẽ làm cho nhân sự bị rối."* Điểm danh làm được ở Vận hành lớp, màn
   Điểm danh, Việc hôm nay, Trợ lý - mỗi nơi một bản dựng riêng.

Cả hai là **ràng buộc cứng của V2** (việc #128), không vá lẻ ở bản này.

### 9d. Ba quan sát cũ vẫn còn, chờ anh quyết

- NV Tư vấn và TP ACA chỉ với tới **1 nút hành động** trên trang đáp của họ.
- NV Marketing thấy **89 việc của người khác** so với 30 việc của chính mình (gấp 3 lần).
- Một giáo viên ở Cơ sở 3 có **mục Giao việc mở ra trống** - cần sửa ở `seed_giaoviec.py`,
  bấm Reset demo không chữa được.

### 9e. Máy vẫn chưa với tới

- **Câu chữ có ĐÚNG NGHIỆP VỤ không** - máy chỉ đo được độ dài, cỡ chữ, có bị cắt không. Việc
  một câu nói đúng hay sai vẫn phải người đọc. (Hôm nay anh Luân bắt đúng loại này: *"Thi giữa
  với thi cuối gì em, phải là Midterm và Final Test"*.)
- **Đẹp hay xấu.** Máy đo được "không bị cắt, không che nhau, không lệch thang" - không đo được
  "nhìn có chuyên nghiệp không".
- **Dữ liệu demo có GIỐNG THẬT không.** Máy canh được tính nhất quán, không canh được chuyện
  một trung tâm thật có bao nhiêu lớp 1-1, tỷ lệ đạt AIM thật là bao nhiêu.
