# BẢNG AUDIT - 05/08/2026 (bản V9.99z9)

> Anh Luân đặt: *"sau khi xong hết, em audit toàn diện cho anh nhé. **Nhớ kiểm tra logic nghiệp
> vụ khi phối hợp nhiều người nha.** Ví dụ: học viên gửi xin nghỉ học, thì tiếp theo là gì, ai
> duyệt, rồi thế nào thế nào.... Tương tự cho các nghiệp vụ khác."*

Chạy trọn `GIAO_THUC_AUDIT.md` (9 mảng) **cộng một mảng thứ 10 mới dựng** cho đúng chỗ anh nhấn
mạnh: chuỗi việc đi qua tay nhiều người.

Cách đọc: mảng nào **XANH** là máy đo được và đo sạch; mảng nào có findings thì ghi rõ đã sửa
hay chưa; mục 9 khai thẳng phần **chưa đo được**.

---

## Bảng tổng kết

| # | Mảng | Kết quả | Đo bằng |
|---|---|---|---|
| 1 | Giao diện | **XANH** | `_checkui` 1048 lượt mở thật · `_checkmat` 14 trang / 1002 chuỗi chữ · `_checkux` 211 tiêu chí · `_check18` 179 · `_tall` 225 icon |
| 2 | Nghiệp vụ (phủ SOP) | **XANH** | `check_sop.py` ĐẠT - 357 cột · 93 trigger HD3 · 51 chỉ số BC2 · 31 hành động CH3 · 12 màn VH + 9 bảng BC |
| 3 | Chức năng | **XANH** (sau khi vá 2 chỗ) | `_checknv` 101 lượt làm thật · `_checkbam` · `_checknguoi` 17 chức danh · `_check12` · `_check17` 413 · `_checkneo` 87 bước · `_checktour` · `_checkqa` 171 |
| 4 | Dữ liệu demo + nút Reset | **XANH** (sau khi vá 1 lỗi nặng) | `check_logic.py` · `check_data.py` · `_checkdemo` 7 thứ × 9 chức danh · `_checkreset` 15 tiêu chí |
| 5 | Ngữ cảnh (câu chữ, drawer, nhật ký) | **XANH** (sau khi vá 1 chỗ) | `_checkaudit` 55 · `_checkux` 87 form · `_check13` 174 · `_check15` 142 cửa ghi |
| 6 | Phạm vi dữ liệu & rò rỉ | **XANH** (sau khi vá 2 chỗ) | `_checkmien` 0/0 · `_checknguoi` · `_check12` |
| 7 | Nhất quán ba cổng | **XANH** | `_check14` 215 tiêu chí (cổng HV + cổng phụ huynh) |
| 8 | Tài liệu theo kịp | **XANH** | `_checkaudit` canh README_SRC nhắc đủ 29 bộ kiểm; 02_NHẬT_KÝ + README app đã có mục V9.99z5→z9 |
| 9 | Chưa đo được | **KHAI THẲNG** | xem mục 9 |
| **10** | **Chuỗi phối hợp nhiều người** *(mới - anh Luân đặt 05/08)* | **XANH** (sau khi vá 1 mắt xích đứt) | `_checkchuoi` - 6 chuỗi × 6 mắt xích = 31 tiêu chí, chạy thật |

---

## Mảng 10 - CHUỖI PHỐI HỢP NHIỀU NGƯỜI (mới)

Mọi bộ kiểm trước đó soi **từng màn của từng người**. Không cái nào đi **hết một việc đi qua tay
hai ba người** - mà đó mới là chỗ dễ đứt: đơn gửi lên rồi nằm im vì không ai thấy; duyệt xong
người gửi không biết; sai người vẫn bấm được nút duyệt.

Mỗi chuỗi đo **sáu mắt xích**, chạy thật trên dữ liệu thật:

1. **có cửa gửi** - người khởi xướng ghi được một dòng thật
2. **vào đúng hàng chờ** - dòng ấy xuất hiện trong hàng chờ của người phải xử lý
3. **đúng người thấy** - người có thẩm quyền thấy, người không liên quan KHÔNG thấy
4. **có nhắc** - chuông/SLA của người phải xử lý đếm việc này
5. **xử lý được** - bấm nút thì trạng thái đổi thật, RỜI hàng chờ, có ghi ai làm lúc nào
6. **người gửi biết** - quay lại cổng của người gửi thì đọc được kết quả

| Chuỗi | Ai qua tay | Kết quả |
|---|---|---|
| Học viên xin nghỉ một buổi | HV → hàng chờ Duyệt nghỉ → Học vụ duyệt → HV đọc kết quả | 8/8 mắt xích |
| Học viên gửi yêu cầu / câu hỏi | HV → hệ thống tự chọn đúng bộ phận (tiền → kế toán, còn lại → học vụ) → nhận → báo xong → HV đọc | 7/7 |
| Thu tiền rồi đối soát | Người thu ghi khoản → hàng chờ của Kế toán → kế toán xác nhận → rời hàng chờ | 5/5 |
| Chiết khấu vượt mức | Tư vấn đề xuất → CHỈ Trưởng phòng Tư vấn duyệt được (kế toán và NV tư vấn bị chặn) | 2/2 |
| Giao việc nội bộ | Người giao → người nhận NHẬN → BÁO XONG → người giao XÁC NHẬN mới khép | 6/6 |
| Khiếu nại | HV gửi → CSKH tiếp nhận → phân công người xử lý → đóng phiếu theo quyền CH3 | 4/4 |

**Bắt được ngay lần chạy đầu - một mắt xích đứt thật:**

> Ô **"Chờ tôi xác nhận (N)"** trên trang Quản lý việc giao & nhận dẫn sang nhóm lọc **"Đang
> chạy"** - mà nhóm ấy chỉ có việc *mới giao* + *đã nhận*, **KHÔNG có việc *đã báo xong***.
> Nghĩa là mắt xích CUỐI của chuỗi giao việc (người giao xác nhận) **không có cửa nào dẫn tới**:
> bấm vào ô đếm 3 việc thì mở ra một danh sách không có ba việc ấy. Đã thêm nhóm lọc "Chờ tôi
> xác nhận" riêng.

---

## Những chỗ hỏng bắt được trong đợt audit này (đã sửa hết)

### Nặng nhất - nút Reset demo (mảng 4)
> **Lần sửa cấu hình ĐẦU TIÊN của một phiên không bao giờ được lưu.** Mốc so sánh của bộ lưu
> cấu hình (`__cfbase`) được đặt LƯỜI - ngay trên chính lần sửa đầu tiên - nên thứ vừa sửa bị
> lấy làm "mốc gốc" và không có gì để ghi xuống `CFKEY`. Bấm Reset demo: `LSKEY` bị xoá, `CFKEY`
> chưa từng được ghi → **sửa gì cũng mất**, trong khi hộp xác nhận vừa hứa "thương hiệu không
> mất". Nay mốc đặt ngay lúc nạp app.

Đây đúng là nút anh Luân nói *"trước khi giao a sẽ bấm nút này đấy"*.

### Do chính đợt sửa hôm nay gây ra (bắt được và vá trong cùng ngày)
- **`go()` tự đổi tab** - luật "bấm tên hub thì mở tab mặc định" nằm trong `go()`, nên nó đè lên
  MỌI lời gọi có đặt tab sẵn: bài hướng dẫn của NV WOW và **trang đáp của cả nhóm WOW** đều bị
  kéo về tab "Lớp học". Đã tách ra `goHub()` - `go()` là bộ định tuyến, không được tự ý đổi tab.

### Sidebar (anh Luân: *"mấy cái bên sidebar lỗi nhiều chỗ"*)
- Hub Học tập có **7 tab mà menu chỉ dẫn tới 3**; hub CSKH có **4 tab mà menu chỉ có tên hub**.
- `navGroupOf` mở **nhầm nhóm**: bấm CSKH thì app mở nhóm "Làm việc" còn nhóm thật sự chứa CSKH
  vẫn gập → không mục nào sáng.
- `_bamGoc` chốt SAU phép remap → bấm Test / Tư vấn / Thanh toán / Chăm lại bị đẩy về tab Lead.
- Mục "Bảng công" bấm vào là menu tối thui (chưa được coi là tab của trang Giảng viên).

### "Bấm menu không ăn" (anh Luân báo 3 lần)
Đo bằng chuột thật 120 lượt: **app có chạy** - nhưng ba thứ đập vào mắt trước nhất (tiêu đề
trang, câu mở đầu, dải phễu) **y hệt nhau ở cả năm tab của hub**. Đổi mà phần đầu màn hình không
đổi thì mắt đọc ra là "không có gì xảy ra". Nay tiêu đề gọi tên tab ("Tuyển sinh · Test đầu
vào"), mục vừa bấm loé một cái, và **chân menu có dấu bản dựng** để hai bên biết chắc đang nhìn
cùng một bản.

### Phạm vi dữ liệu (mảng 6)
- **Bảng công giảng dạy** hiện đơn giá giờ / đơn giá WOW / tiền công tạm tính cho người khai
  `tien:"none"`. Nay ai không có miền tiền thì bảng chỉ đếm công.
- **NV Marketing mở app thấy 42 việc, cả 42 đều của Tư vấn, 0 việc của chính họ.** CH3 giao cho
  Marketing hai việc: nhập lead mới và **chăm lại khách cũ** - mà màn "Chăm lại / Reup" có,
  badge menu đếm đúng, nhưng **không một luật SLA nào sinh ra việc**. Nay có hàng chờ thật
  "Chăm lại tới hẹn".
- Giáo viên và đội WOW không còn tab "GV dự phòng" / "Phòng & đụng lịch" - đó là cửa ghi của
  Học vụ và Trưởng phòng ACA (mời rồi đuổi còn tệ hơn không mời).

### Ngữ cảnh & cửa ghi (mảng 5)
- **Cửa ghi khoản thu không vào nhật ký DL25** và trong nhánh thường cũng không nói một câu nào -
  người thật bấm xong không biết đã ghi được chưa nên bấm lại. Nay có cả hai.
- Khung nhập bản ghi của danh sách **nhúng** dựng sẵn ở trạng thái ẩn → một nút "Lưu bản ghi"
  nằm chết trong DOM không có lối vào.
- Gõ tìm trong danh sách nhúng **nuốt cả trang**, thay hub bằng sổ dữ liệu thô.
- "Bộ phận" → **"Mảng việc"**: dải lọc có 7 giá trị, 6 là bộ phận thật nhưng "Giao việc" không
  phải phòng ban nào.

### Suýt mất trong im lặng
**Nhịp ngày theo chức danh** biến mất khỏi app khi gỡ phần xử lý việc khỏi Trợ lý - `nhipList()`
còn nguyên, còn cấu hình được, mà không hàm nào gọi tới. Nay nằm ở trang **Việc hôm nay**.

---

## 9. NHỮNG THỨ MÁY KHÔNG ĐO ĐƯỢC - khai thẳng

1. **Câu chữ có tự nhiên không.** Máy đếm được "có câu giải thích", không đọc được "câu này có
   dễ hiểu với người lần đầu không". Phải người thật đọc.
2. **Nhịp làm việc thật.** `_checkngay` đo được "có việc để làm", không đo được "một ngày của
   NV tư vấn có trôi chảy không".
3. **Đẹp.** `_checkui`/`_checkmat` đo "không vỡ, không cắt chữ, không che nhau" - không đo được
   nhịp đọc và thẩm mỹ. Ảnh chụp 8 trang × 2 tỷ lệ có sẵn bằng `_anhman.js`, nhưng phần nhìn là
   của người.
4. **Bản demo chạy trên GitHub Pages.** Em **không fetch được** trang live để tự kiểm tra bản
   đang phục vụ (proxy của môi trường chặn, HTTP 403) - nên khi anh báo lỗi, phải đọc **dấu bản
   dựng ở chân menu** mới biết chắc hai bên nhìn cùng một bản.
5. **Một người dùng thật, nhiều máy.** App không có backend: dữ liệu nằm trong localStorage của
   TỪNG máy, phân quyền thi hành ở trình duyệt. Không đo được chuyện hai người cùng sửa một hồ sơ.
6. **Dữ liệu là dữ liệu mẫu.** Mọi con số trong bảng trên là của bộ demo 36 nhân sự / 82 học
   viên / 22 lớp / 6 nơi học, không phải số thật của trung tâm.
7. **Bản v6 - anh Luân chốt BỎ HẲN (05/08):** *"ko cần sửa đâu, a bỏ luôn bản v6 đây"*. Chỗ
   em khai lúc đầu (Bàn làm việc theo thực thể của v6: hồ sơ Marketing còn 86 việc của người
   khác so với 26 của họ) vì thế **không còn là việc phải vá**. Bản giao là V5. Mã v6 còn nằm
   trong nguồn - việc dọn ghi ở VIỆC TỒN.
8. **Một chỗ còn ghi chú (không phải lỗi):** giáo viên Trịnh Quốc Bảo mở mục "Quản lý việc giao
   & nhận" ra thấy trống - anh ấy đúng là không có việc nội bộ nào trong bộ demo. Trang nói rõ
   "Bạn đang sạch việc nội bộ" chứ không để trống trơn, nên em xếp vào diện khai chứ không vá
   bằng cách nhét việc giả cho đủ.
