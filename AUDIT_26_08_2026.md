# AUDIT 26/08/2026 - "ĐÃ CÓ NHƯNG LÀM CHƯA TỚI"

> Anh Luân: *"hôm nay a ko bổ sung chức năng gì, em cứ audit toàn diện để fix những cái đã có
> nhưng làm chưa tới đi"*

Chạy trọn `GIAO_THUC_AUDIT.md`. Khác mọi lần trước ở chỗ: **không thêm tính năng nào**, chỉ đi
tìm những thứ app đã làm mà làm nửa chừng. Bản dựng chốt: `9786b3`.

Điểm xuất phát khác thường: 47 bộ kiểm đang **XANH HẾT**. Nên vòng đầu tiên là đi gom lại toàn
bộ **"ghi chú"** mà chính các bộ kiểm đang hạ xuống mức không-đỏ - đó là chỗ máy đã biết là chưa
tới nhưng không có quyền chặn.

---

## Phép đo mới dựng cho lần audit này

| Phép đo | Hỏi gì | Kết quả lần đầu |
|---|---|---|
| Khối rỗng theo từng người | Đóng vai 33 người, vẽ **729 lượt trang**, đếm khối rỗng | 24 khối rỗng trên nhiều người |
| Bảng rỗng theo từng người | Mọi bảng trên trang đều rỗng | `giangvien` 80% · `magioithieu` 92% · `banggiao` 54% |
| Trang trên menu mà miền dữ liệu là "none" | Menu có mời vào chỗ không xem được không | 2 cụm |
| Ngăn kéo cụt | Bấm một dòng sổ ra ngăn kéo **không nút nào** | 1 lớp lỗi, phủ mọi sổ chỉ-xem |
| Trục lọc chết | Trục khai ra mà không lọc được gì | 3 trục một giá trị · 19 trục có nút tên "(trống)" |
| Cột luôn rỗng | Cột khai mà 0/N dòng có dữ liệu | **0/238 cột** - sạch |
| Xuất CSV vs cột đang hiện | Xuất có đúng cột người ta thấy không | Khớp - sạch |

---

## Mười chỗ đã sửa

### 1. Trang Giảng viên rỗng với 24/30 người có nó trên menu
Toàn bộ giáo viên, NV WOW, học vụ, kế toán và phần lớn tư vấn mở ra thấy *"Chưa có bản ghi nào
trong bảng này"* - trong khi dải thẻ **ngay phía trên** vẫn ghi "6 GV · tổng 122.6h" và "Lớp nào
cũng đã có giảng viên". Trang tự cãi chính nó.

Gốc: sổ lấy dòng theo miền **nhân sự** (`DSDOM.DL01`), mà miền ấy khai `none` cho gần hết mọi
người - đúng, vì DL01 **còn** chứa lương và hợp đồng. Nhưng cuốn sổ này không hiện một chữ nào
của những thứ đó: cột của nó là lớp đang dạy · học viên · nợ nhận xét · bài chờ chấm.

Chữa: khai `mien:"lop"` cho riêng sổ ấy, thêm `scopeList(code,arr,mienOv)` + `canRow(...,mienOv)`
+ `listRows(key)` để **bốn** chỗ dựng lại danh sách gốc (thân trang · hộp lọc sâu · bộ đếm chip ·
bộ đếm menu) cùng đi một cửa.

> *Một bảng có thể trả lời hai câu hỏi khác nhau; khoá nó theo câu hỏi kia là chặn mất câu này.*

### 2. Trang Mã giới thiệu: chỉ Giám đốc thấy được dòng nào
12/13 người thấy `0 mã` - **cả ba người Marketing**, tức chính chủ của chương trình, và cả bảy tư
vấn viên. Hai lỗi chồng nhau:

- `recOwners` xếp DL19 và DL22 chung nhánh với **DL06**, mà nhánh ấy hỏi `r.lead_id` và
  `r.created_by` - **hai bảng giới thiệu không có cột nào trong hai cột đó** (DL22 gọi nó là
  `referred_lead_id`). Bản khai chủ sở hữu trả về mảng rỗng vĩnh viễn: không lỗi, không cảnh báo.
- Và bảng bị khoá theo miền **tiền**. DL19 không giữ một con số tiền nào (phần thưởng là hiện
  vật); DL22 có `friend_discount_amount` nhưng cột ấy **đã** được che riêng.

> *Che đúng một cột tiền rồi để cả bảng chết theo miền tiền thì người ta không thấy cột nào hết.*

### 3. Menu mời vào trang mà người ta không xem được dữ liệu
Luật *"mời rồi đuổi còn tệ hơn không mời"* đã đặt hai lần, cả hai lần đều **gọi đích danh một
trang** (`bangcong`, rồi `baocao`). Trang thứ ba mắc đúng bệnh thì không ai hỏi tới: Marketing có
mục "Học viên" trên menu với `hocvien:"none"`. Nay `navVis` hỏi chung.

### 4. Bàn giao lead luôn mở ở một cái bảng rỗng
Trang mở ở sổ của **chính người đang xem** - đúng với tư vấn viên, nhưng người có ô chọn lại là
quản lý và Marketing, hai nhóm không ai giữ lead. Nay mở ở nguồn CÓ dòng (của mình → vô chủ →
người ôm nhiều nhất), và ô chọn mang theo số lead từng mục.

### 5. Ngăn kéo của dòng sổ là ngõ cụt
`lstXem` đi tìm học viên, khách và lớp - **không câu nào hỏi tới nhân sự**. Nên bấm một giảng
viên ra một tờ khai cột với câu "sổ này chỉ để tra cứu", dù app có sẵn hồ sơ đầy đủ. `_checkbam`
ghi chú ba lần ở mức không-đỏ. Sửa xong đo lại: **0 ngõ cụt trên toàn bộ LISTCFG**.

### 6. Trục lọc: nhóm lớn nhất không có tên
19 trục bày ra một nút tên **"(trống)"**. Ở trang Buổi học nút ấy gom **260 buổi** - tức "buổi
chưa ghi nhận xét", đúng nhóm học vụ mở bộ lọc ra để tìm. `fxStaff`/`fxRef` đã đặt tên cho ô
trống từ lâu; riêng `fxEnum` thì không.

> *Một nhóm không có tên thì không ai đi làm nó.*

### 7. Ba trục lọc chỉ có một giá trị
Bấm hay không bấm đều ra cùng kết quả. Ẩn theo **dữ liệu đang có**, không xoá lời khai.

### 8. Bảng rỗng nói sai lý do nó rỗng
Câu "chưa có bản ghi nào trong bảng này" nói sai khi trung tâm có đầy bản ghi mà không bản nào
thuộc phạm vi người đọc. Nay có nhánh thứ ba nói đúng chuyện đó.

### 9. Một khái niệm, hai phép đếm - "thưởng giới thiệu còn treo"
Lộ ra ngay sau khi mở đúng miền dữ liệu cho Marketing (`_checkcauhoi` bắt). `mkThuongTreo()` đọc
`rows` trong khi hai nơi kia đọc `srows`, **và** định nghĩa "còn treo" bằng `granted_at` rỗng
trong khi hai nơi kia hỏi `reward_status=pending`. Trước đó bị che vì Marketing khai `tien:"none"`
nên `srows("DL19")` luôn bằng 0 - hai con số là 2 và 0, đủ khác nhau để không ai đối chiếu.

Và một tầng nữa: nhịp đếm **phần thưởng** (2), chip lọc bảng "học viên chủ mã" nên đếm **người**
(1). Cả hai đều đúng; cái sai là chúng đứng cạnh nhau mà không nói mình đang đếm gì.

> *Sửa một chỗ sai làm lộ ra chỗ sai thứ hai không phải là làm hỏng thêm - đó là chỗ sai thứ hai
> thôi hết chỗ nấp.*

### 10. Phòng Nhân sự có 0 việc của chính mình
`_checkngay` đo ra cả ba chức danh Nhân sự có 0 việc. Hai nguyên nhân:
- Hàng chờ "Hồ sơ nhân sự còn thiếu" chỉ hỏi **hai câu** nên khớp 0/36 dòng. Nay hỏi thêm email
  và số điện thoại, và nói luôn **thiếu gì**.
- Hai ô biên chế chưa tuyển được người (`notes="vị trí trống"`) đang đội lốt "Đã nghỉ việc" và
  **không một màn nào trong app nói tới** - trong khi "vị trí nào đang trống" là câu hỏi đầu tiên
  của một phòng Nhân sự. Thêm ô thẻ "Vị trí đang trống".

### + Dữ liệu demo: một giáo viên không có học viên nào
Lương Bảo Ngọc phụ trách đúng hai lớp, một "Đang tuyển sinh" một "Đang lên kế hoạch", **cả hai
không có học viên nào** → đăng nhập bằng người này là cả app trống. Chữa ở nguồn pipeline
(`fixdata` khối 28) bằng cách **đổi giáo viên phụ trách** một lớp đang chạy - buổi đã dạy giữ
nguyên tên người dạy thật, chỉ buổi từ hôm nay trở đi mới sang tên.

---

## Đo lại sau khi sửa

| Trang | Tỷ lệ người mở ra thấy mọi bảng đều rỗng |
|---|---|
| `giangvien` | 80% → **0%** |
| `magioithieu` | 92% → **8%** |
| `banggiao` | 54% → **15%** |
| `duyet` | 44% → **22%** |
| `hocvien` | 13% → **4%** |

Phần còn lại là **dữ liệu thưa thật** (người chưa có tin nhắn nào, lớp chưa tới lịch tuần này),
không phải lỗi - và mỗi bảng ấy nay tự nói ra vì sao nó rỗng.

---

## Bảng kết quả 9 mảng

| Mảng | Kết quả |
|---|---|
| 1. Giao diện | **XANH** - `_checkui` 1263 lượt · `_checkmat` 54 trang × 2 khổ, 10 phép đo · `_checkdrawer` 56 ngăn kéo |
| 2. Nghiệp vụ | **XANH** - `check_sop.py` phủ trọn bốn mặt; thêm một hàng chờ SOP mô tả mà app chưa có (vị trí trống) |
| 3. Chức năng | **ĐÃ SỬA 6 CHỖ** - xem mục 1-5 và 9 |
| 4. Dữ liệu demo + Reset | **ĐÃ SỬA 1 CHỖ** - giáo viên không có học viên; `check_data`/`check_logic`/`_checkreset` đều ĐẠT |
| 5. Ngữ cảnh | **ĐÃ SỬA 3 CHỖ** - nhãn "(trống)" · bảng rỗng nói sai lý do · ô thẻ chỉ đếm mà không nói thiếu gì |
| 6. Phạm vi dữ liệu & rò rỉ | **ĐÃ SỬA 3 CHỖ** - và đây là mảng nặng nhất lần này: hai miền khai sai, một bản khai chủ sở hữu chết |
| 7. Nhất quán ba cổng | **XANH** - `_checkcong` 25 học viên × 5 câu hỏi |
| 8. Tài liệu | **ĐÃ CẬP NHẬT** - 02, README_SRC, README app, và bản này |
| 9. Chưa đo được | Khai ở dưới |

---

## Mảng 9 - khai thẳng phần chưa đo được

- **Ba lần trong lần audit này em phải nghi cái thước trước, và cả ba lần thước sai:** phép đo
  trục lọc đọc nhầm cách khai `fxCalc` · rồi tự bỏ giá trị trống trong khi hộp lọc vẫn đếm nó ·
  và một lần đếm dòng bằng hàm không nhận tên sổ. Nếu tin lần đo đầu thì đã đi "sửa" ba chỗ app
  hoàn toàn đúng.
- **Trục "dạy thay" chưa được đo:** `stuOwners` tính chủ hồ sơ học viên theo **giáo viên chính
  của lớp**, không tính người **thật sự đứng lớp** buổi đó. Hôm nay chưa có ca nào như vậy trong
  dữ liệu (DL31 mới có đăng ký khung giờ, chưa gán buổi) nên không đo được hệ quả. Tiền lệ đã có:
  luật "người kèm WOW 1-1 cũng là chủ hồ sơ" chính là cùng một câu hỏi, đã trả lời cho WOW mà chưa
  trả lời cho lớp.
- **`_checkngay` vẫn ghi 6 chỗ "cần xem thêm"** - việc của bộ phận khác nhiều hơn việc của chính
  mình trên hồ sơ của một số chức danh. Chưa quyết đó là lỗi hay là đúng bản chất (một giáo viên
  mở hồ sơ học viên ra thì phần lớn thông tin trên đó vốn là của phòng khác).
- **Giáo viên Bùi Thị Ngọc Hân còn đúng 1 học viên.** Khối 28 chỉ vá cho người có **0** học viên -
  người có 1 vẫn có lớp, có buổi, có bài để chấm nên app của họ không trống. Không kéo thêm để
  tránh xáo trộn dữ liệu mà không cứu ai.
- **Hai chỗ hình học còn treo từ 25/08** (`hanhtrinh` đè 18×14px · `chang` nhãn `.ajt` bị bóp còn
  57px ở khổ điện thoại) - chưa hiểu cơ chế, không khai ngoại lệ.
- Giới hạn còn tồn của bản demo: không backend · dữ liệu nằm trong localStorage từng máy · phân
  quyền thi hành ở trình duyệt · dữ liệu là dữ liệu mẫu.
