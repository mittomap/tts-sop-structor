# BẢNG AUDIT 9 MẢNG - 25/08/2026

> Anh Luân: *"xử lý toàn bộ việc tồn, audit toàn diện, chủ động fix sạch toàn bộ. Kiểm tra ngữ
> cảnh, giao diện, tính năng, độ phủ sop và mọi thứ. Cố gắng hoàn chỉnh ở mức final."*

Chạy trọn `GIAO_THUC_AUDIT.md`. Bản dựng audit: **`ba3d7a`**, 47 bộ kiểm.

---

## Tóm tắt: mảng nào xanh, mảng nào đỏ, mảng nào CHƯA ĐO ĐƯỢC

| # | Mảng | Kết quả |
|---|---|---|
| 1 | Giao diện | 🟢 xanh - sau khi vá **21 chỗ** mà bộ kiểm cũ chưa từng nhìn |
| 2 | Nghiệp vụ (độ phủ SOP) | 🟢 xanh - `check_sop.py` ĐẠT trên cả bốn mặt |
| 3 | Chức năng | 🟢 xanh |
| 4 | Dữ liệu demo + nút Reset | 🟢 xanh - đã bấm Reset thật, sau khi phá dữ liệu |
| 5 | Ngữ cảnh (câu chữ, ngăn kéo) | 🟢 xanh - đọc tay 7 ngăn kéo, vá 1 chỗ |
| 6 | Phạm vi dữ liệu & rò rỉ | 🟢 xanh |
| 7 | Nhất quán ba cổng | 🟢 xanh - **vừa có bộ kiểm đầu tiên** (`_checkcong`) |
| 8 | Tài liệu theo kịp | 🟢 xanh - VIỆC TỒN đang khai sai 4/8 mục, đã sửa |
| 9 | Chưa đo được | 🟡 **khai thẳng bên dưới** |

---

## 1. GIAO DIỆN

Bộ kiểm hình học `_checkmat` trước audit chỉ đi qua **19 trên 52 trang**. Đếm ra con số ấy là
việc đầu tiên, vì ba lần trước bài học đều được ghi là *"danh sách phải phủ đủ kiểu bố cục"* -
một câu tự trấn an không kiểm chứng được.

Mở rộng sang 52 trang: **24 chỗ đỏ**.

**21 chỗ thật, đã vá:**

| Chỗ | Đo được | Cách chữa |
|---|---|---|
| Màn Chấm bài | dùng 63% bề ngang, mọi dòng cụt, ô điểm hiện "Banc" | tách 4 cột, 95% |
| Màn Thu bài | bỏ trống 507/1100px mà vẫn cắt chữ | tách 4 cột, 97% |
| Dải chặng trong thẻ hồ sơ | rộng 341px trong cột 250px, hai dải chồng nhau | cho co + chặn tràn |
| Nhãn chặng | cắt 8-53px | cho xuống hai dòng |
| Tên trong thẻ chọn người | cắt 4-10px | cho xuống dòng |
| Ô chọn tuần | hụt 4px trong khi hàng thừa 884px | nâng sàn 224→264px |
| Ô hỏi Trợ lý (điện thoại) | câu mời hụt 83px | rút câu ở khổ hẹp |
| Cột "Họ tên" bảng Công nợ | 90px, tên vỡ 2 dòng, trong khi cột "Khoá" 253px | sàn 132px |
| Vệt đường đi | 5 mốc trong 496px, 3 mốc cắt 6-21px | 4 mốc, cắt nhãn theo chữ |
| Kho câu hỏi gợi ý | 9 hàng tự đo cột riêng, dòng tràn rớt về mép trái | một lưới hai cột |
| Menu | cấp con sáng hơn cấp cha, thụt lề không có bậc | dựng lại thứ bậc |

**3 chỗ là lỗi của chính thước, đã sửa thước** - đáng nhớ hơn 21 chỗ trên:

- **M1 đo bề rộng chữ của Ô TÍCH.** `value` của checkbox là mã gửi đi, không phải chữ hiện ra
  màn. 4 dòng oan trên trang Bàn giao lead.
- **M2 chấm một link TRÔI HAI DÒNG là "bị phủ lên".** Hộp của nó là hộp gộp trùm cả hai dòng nên
  điểm giữa rơi vào khoảng giữa - nơi phần tử đứng sau đang nằm. **Bẫy này đã được ghi ngay trong
  file ấy cho M5 và có sẵn cách chữa, nhưng chỉ gắn cho M5.**
  *Ghi lại một cái bẫy ở một chỗ không làm nó biến mất ở chỗ khác.*

`_checkmat` nay: **54 mục × 2 khổ màn, 9.388 chuỗi chữ** (trước: 24 mục, 3.062 chuỗi).

## 2. NGHIỆP VỤ - ĐỘ PHỦ SOP

`check_sop.py` đọc thẳng `ITTs_Operations_Template_v4.xlsx`, **ĐẠT** cả bốn mặt: 357 cột DL ·
93 tình huống trigger HD3 · 51 chỉ số BC2 · 31 hành động CH3 · 12 màn VH + 9 bảng BC · thuật ngữ
CH5 · bốn sheet cấu hình CH1/CH2/CH4/CH6.

**Thêm theo LUẬT CỨNG SỐ 0** (thấy SOP mô tả mà app chưa có thì làm): trục **"giáo viên nghỉ liên
tiếp N buổi"** và **"nghỉ nhiều lần"** cho hàng chờ DL33 - hai câu người duyệt hỏi ngay sau câu
"ai nghỉ buổi nào", mà trước bản này hàng chờ không trả lời được câu nào.

## 3. CHỨC NĂNG

Toàn bộ 47 bộ kiểm chạy. Đáng chú ý: `_checkghe` (ngồi vào ghế từng chức danh, hỏi *"cái nút này
có đúng người được bấm không"*) và `_checkbc` (mỗi ghế một bản báo cáo) đều xanh.

**Một lỗi thật tìm ra:** trên Công nợ / Thanh toán / Sổ thu, **tên khách chưa thành học viên
không bấm được** - 10 chỗ. Họ đã đăng ký và đang nợ tiền nhưng `student_id` rỗng, chỉ có
`lead_id`. *Một dòng không mở ra được cái gì là một ngõ cụt, và ngõ cụt trên bảng công nợ nghĩa
là người đi đòi tiền phải tự đi tìm hồ sơ ở trang khác.*

Chữa ở ba tầng: `CELLLNK` khai được **mã dự phòng** (mọi sổ qua `renderList` tự được hưởng) · 11
chỗ gọi `nguoiLnk` trên dòng đơn/phiếu · `cnDs` nhớ lại cả mã lead.

**Và một lỗ của chính bộ kiểm:** `_checklink` chỉ lấy tên từ DL09 nên nó có một vùng tối đúng
bằng cả bảng DL02. *Một bộ kiểm hỏi "mọi tên có bấm được không" mà chỉ lấy tên từ một bảng thì nó
đang hỏi "mọi tên TRONG BẢNG ẤY".*

## 4. DỮ LIỆU DEMO VÀ NÚT RESET DEMO

Đây là chỗ anh Luân sẽ bấm trước khi giao. **Đã bấm thật, sau khi cố tình phá:** xoá 40 học viên,
xoá sạch bảng DL33, xoá 200 bài tập.

| Hỏi | Sau khi bấm Reset |
|---|---|
| Dữ liệu về đủ chưa | 45 → **85 học viên**, 0 → **6 đơn báo nghỉ**, 26 lớp, 608 buổi, 384 bài, 87 buổi WOW |
| Ngày tháng kéo về hiện tại chưa | **5 buổi học hôm nay**, **166 việc quá hạn** để thấy cảnh báo đỏ |
| Mọi chức danh có việc chưa | **17/17 ghế**, không ghế nào bảng trống |
| Cấu hình có bị cuốn theo không | không |
| Tỷ lệ hiển thị có bị xoá không | **không** - giữ nguyên 80% |
| Lỗi JS | không có |

> **Một lượt đo sai của em, phải khai:** lượt đầu em đọc ra "tỷ lệ hiển thị bị xoá" và suýt báo
> đó là lỗi. Đo lại cẩn thận thì thước sai - nó đọc localStorage lúc trang đang tải lại. Đúng luật
> mở đầu giao thức: *nghi cái thước trước, đừng nghi app trước.*

**Một lỗi dữ liệu thật:** lớp `LOP-IELTS-6.5-04` có **ba buổi cùng ngày lúc 17:00, 18:00, 19:00** -
chồng lên chính nó. Khối 23 của pipeline đã gỡ hết chỗ trùng giờ, rồi NA068/NA069 mới dời buổi về
"45 phút trước" - và cửa chặn của chúng so chuỗi ngày-giờ **khít từng phút**, trong khi app coi
một buổi chiếm **2 tiếng**.
*Hai nơi cùng nói về "trùng giờ" mà đo bằng hai thước khác nhau thì nơi nào đổi sau sẽ phá việc
của nơi trước, và không ai thấy mình vừa phá.*
`check_data.py` và `check_logic.py` đều ĐẠT - chúng không hỏi câu ấy; `_check16` bắt.

## 5. NGỮ CẢNH

**Đọc tay 7 ngăn kéo** ở bảy nghiệp vụ khác nhau (hồ sơ học viên · khách tiềm năng · lớp · nhân
sự · duyệt GV báo nghỉ · khiếu nại · chỉ số KPI): không chỗ nào lọt `undefined` / `NaN` /
`[object Object]`, đều có nút đi tiếp, độ dài 174-1.984 ký tự.

**Một chỗ chữ nghĩa đã vá:** ngăn kéo chỉ số hiện một dấu gạch trơ `–` không lời giải thích, lời
giải thích thì giấu trong chú thích rê chuột. Trên BẢNG thì dấu gạch ấy đúng chỗ (cột hẹp, cả cột
cùng một kiểu), nhưng trong ngăn kéo có cả một khoảng trống bên phải.
*Cùng một thông tin, chỗ chật thì rút thành ký hiệu, chỗ rộng thì nói thành câu.*

## 6. PHẠM VI DỮ LIỆU & RÒ RỈ

Đóng vai **25 người thật trên 17 chức danh**, đếm số dòng mỗi người thấy được ở 5 bảng. Mọi con số
khớp đúng bản khai miền: Marketing thấy 193 lead / 0 học viên · Nhân sự thấy 0 lead / 0 học viên ·
giáo viên thấy học viên lớp mình · ba Sale Leader thấy ba tập khác nhau (31/28/51 học viên).
`_checkmien` và `_checknguoi` xanh.

## 7. NHẤT QUÁN GIỮA BA CỔNG

**Mảng này trước audit chưa có bộ kiểm nào đi qua.** `_check14` soi kênh hai chiều của cổng học
viên, `_checkmien` soi rò dữ liệu, `_check18` soi trùng khối - không bộ nào **đối chiếu** hai bên.

*Đây là loại hỏng im lặng nhất: cả hai cổng đều vẽ được, đều không lỗi, đều có số - chỉ là hai số
khác nhau, và người phát hiện ra sẽ là học viên gọi điện lên hỏi.*

Đã dựng **`_checkcong`** (bộ thứ 47): 25 học viên × 5 câu hỏi = 152 tiêu chí - chuyên cần, bài
tập, công nợ, quota WOW, tên. Đọc số từ HTML của cổng học viên chứ **không** gọi lại hàm: gọi lại
hàm thì hai vế dùng chung một phép tính và bộ kiểm chỉ chứng minh rằng một hàm bằng chính nó.

**Ba lần sửa thước trước khi nó đo đúng**, cả ba cùng một họ (đo chuỗi phẳng thay vì đo cấu trúc)
- ghi lại cả ba trong file. Đã thử phá để chắc nó đỏ được thật.

## 8. TÀI LIỆU CÓ THEO KỊP KHÔNG

**VIỆC TỒN đang khai sai 4/8 mục** - đúng cảnh báo của chính mảng này (*"đây là chỗ dễ nói dối bản
thân nhất"*):

| Mục | Khai là | Đo lại |
|---|---|---|
| Ô số tiền đợt thiếu dấu chấm ngăn nghìn | còn treo | đã xong (`5.600.000`) |
| Ô "Ngày đóng đợt đầu" hiện cả khi đóng một lần | còn treo | đã xong |
| `renderBanlam` nhánh mã chết | còn treo | đã gỡ |
| Menu mở ra trang trống | chờ quyết | đã hết |

*Một danh sách việc tồn không được đọc lại thì nó thôi mô tả hiện tại và bắt đầu mô tả quá khứ -
và người đọc nó sẽ đi làm những việc đã xong.*

`_checkaudit` bắt đúng một chỗ: `README_SRC.md` chưa nhắc bộ kiểm thứ 47. Đã bổ sung.
Ba tài liệu (`02_NHAT_KY`, `README_SRC`, `ITTs_WebApp_v5_README`) đều đã cập nhật.

---

## 9. NHỮNG THỨ MÁY KHÔNG ĐO ĐƯỢC - KHAI THẲNG

### 9a. Còn treo THẬT, đã đo được nhưng chưa chữa xong

1. **`hanhtrinh`: hai chữ đè nhau 18×14px** trong thẻ hồ sơ trên bảng kanban. Đã sửa **bốn lần
   theo chiều ngang** (cho vỏ co · cho tên bước co · chặn tràn · cho dải thành khối riêng) và lần
   nào đo lại cũng y nguyên. Bốn bản vá ấy đều là cải thiện thật và giữ lại, nhưng chỗ đè vẫn còn.
   *Sửa bốn lần không ăn thì đừng sửa lần thứ năm cùng một hướng - em chưa hiểu cơ chế.*
   **Không khai ngoại lệ**: khai ngoại lệ cho một thứ mình chưa hiểu là tắt đèn chứ không phải
   dọn nhà. Hai trang `hanhtrinh` và `chang` tạm chưa vào danh sách `_checkmat`.
2. **`chang` khổ điện thoại**: nhãn `.ajt` bị bóp còn 57px trong khối 177px, vỡ 3 dòng.
3. **`check_taolai` đỏ đúng một lần (15/08) rồi không tái hiện.** Chưa tuyên bố đã sửa. Đã cho nó
   ghi hồ sơ mỗi lần đỏ (`_src/_taolai_khac/`) để lần sau bắt được dấu vết ngay lần đầu.

### 9b. Chỗ chưa có bộ kiểm nào đi qua

- **Nhịp làm việc thật** - app đo được "có việc để làm không", không đo được "làm xong một ngày có
  mệt không". Chỉ người thật dùng mới biết.
- **Câu chữ có tự nhiên không** - `_checkcau` bắt được câu tự mâu thuẫn, `_checkaudit` bắt được
  giọng văn nội bộ lọt ra màn, nhưng không bộ nào đọc được "câu này nghe có giống người Việt nói
  không".
- **Thứ tự đọc trên một màn** - `_checkbc` kiểm được khối đầu tiên có đúng chủ đề của ghế không,
  nhưng không kiểm được cả trang có dẫn mắt hợp lý không.

### 9c. Giới hạn của bản demo (không phải lỗi)

- Không backend - dữ liệu nằm trong localStorage từng máy, 66 chỗ `google.script.run` là đường nối
  ra máy chủ tương lai, `SVR` luôn false.
- Phân quyền thi hành ở trình duyệt - đủ cho demo, không đủ cho bản chạy tiền.
- Dữ liệu là dữ liệu mẫu, dựng lại được bằng pipeline.
- Gửi email/Zalo là **mô phỏng** (soạn → xem lại → lưu DL29), anh Luân đã biết.
- Chặn đăng nhập làm test khi quá hạn 1 tuần: **cố ý không code**, anh Luân chốt *"để a cho dev
  làm sau"*.

---

## Ba việc tay mà máy không làm thay được - đều đã làm

1. **Bấm Reset demo** rồi rà lại mục 4 bằng tay - làm, kèm phá dữ liệu trước.
2. **Đóng vai nhiều chức danh** - 25 người trên 17 ghế, đo phạm vi dữ liệu từng người.
3. **Đọc câu chữ** ở 7 ngăn kéo như người dùng lần đầu - làm, bắt được 1 chỗ.
